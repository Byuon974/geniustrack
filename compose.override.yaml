---
# Development environment override
services:
  php:
    image: ${IMAGES_PREFIX:-}app-php-dev
    build:
      context: .
      target: frankenphp_dev
    volumes:
      - ./:/app
      - ./frankenphp/Caddyfile:/etc/frankenphp/Caddyfile:ro
      - ./frankenphp/conf.d/20-app.dev.ini:/usr/local/etc/php/app.conf.d/20-app.dev.ini:ro
      # Keep var/ off the bind-mount for faster I/O on Mac/Windows; comment to inspect from the host.
      - /app/var
      # If you develop on Mac or Windows you can remove the vendor/ directory
      #  from the bind-mount for better performance by enabling the next line:
      #- /app/vendor
    environment:
      FRANKENPHP_WORKER_CONFIG: watch
      FRANKENPHP_SITE_CONFIG: hot_reload
      MERCURE_EXTRA_DIRECTIVES: demo
      # See https://xdebug.org/docs/all_settings#mode
      XDEBUG_MODE: "${XDEBUG_MODE:-develop}"
      APP_ENV: "${APP_ENV:-dev}"
    extra_hosts:
      # Ensure that host.docker.internal is correctly defined on Linux
      - host.docker.internal:host-gateway
    tty: true
    depends_on:
      database:
        condition: service_healthy
###> symfony/mercure-bundle ###
###< symfony/mercure-bundle ###

  # Le service database n'expose AUCUN port sur l'hôte : l'application lui parle
  # depuis l'intérieur du réseau Docker. Cela évite tout conflit avec un éventuel
  # PostgreSQL déjà installé sur la machine (port 5432 occupé).
  # Si vous voulez inspecter la base depuis votre PC (pgAdmin, DBeaver…), décommentez
  # en choisissant un port libre côté hôte, par exemple 5433 :
  # database:
  #   ports:
  #     - "5433:5432"
